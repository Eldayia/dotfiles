# --- CONFIGURATION HYPRLAND COMPLETE (CORRIGÉE) ---

# 1. ÉCRAN & ÉCHELLE
# Le "1.5" à la fin grossit le texte (1.0 = petit, 1.5 = moyen, 2.0 = gros)
monitor=,preferred,auto,1.3333334

# 2. VARIABLES D'ENVIRONNEMENT (CRITIQUE POUR VM)
env = WLR_NO_HARDWARE_CURSORS,1
env = NIXOS_OZONE_WL,1
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
# Workaround pour VMware - forcer le rendu software si nécessaire
env = WLR_RENDERER_ALLOW_SOFTWARE,1

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = wofi --show drun

# 3. DÉMARRAGE AUTOMATIQUE
# Désactivé temporairement pour diagnostiquer les problèmes de stabilité
# exec-once = waybar
exec-once = dunst               

# 4. CLAVIER ET SOURIS
input {
    kb_layout = fr
    follow_mouse = 1
    sensitivity = 0 
}

# 5. APPARENCE GÉNÉRALE
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    layout = dwindle
}

# 6. DÉCORATION (Nouvelle Syntaxe pour Hyprland v0.40+)
decoration {
    rounding = 10

    # Blur désactivé pour VM (très gourmand)
    blur {
        enabled = false
        size = 3
        passes = 1
    }

    # Ombres légères pour VM
    shadow {
        enabled = true
        range = 2
        render_power = 2
        color = rgba(1a1a1aee)
    }
}

animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    # Animations simplifiées pour VM (moins gourmandes)
    animation = windows, 1, 3, myBezier
    animation = windowsOut, 1, 3, default, popin 80%
    animation = border, 1, 5, default
    animation = fade, 1, 3, default
    animation = workspaces, 1, 3, default
}

dwindle {
    pseudotile = true
    preserve_split = true
}

# 7. RACCOURCIS CLAVIER
$mainMod = CTRL

# Applications
bind = $mainMod, T, exec, kitty
bind = $mainMod, Q, exec, kitty
bind = $mainMod, B, exec, microsoft-edge
bind = $mainMod, E, exec, kitty -e yazi
bind = $mainMod, space, exec, wofi --show drun -i
# Lancer waybar manuellement
bind = $mainMod, W, exec, waybar
# Terminal de secours
bind = $mainMod, X, exec, xterm
# Script de diagnostic
bind = $mainMod, D, exec, bash ~/.config/hypr/debug-env.sh

# Gestion Fenêtres
bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo,
bind = $mainMod, J, togglesplit,

# Focus (Flèches)
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Workspaces 1-5
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5

# Déplacer vers Workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5

# Souris
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow